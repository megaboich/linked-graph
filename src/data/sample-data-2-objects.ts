export function get2ObjectsGraphData() {
  const name = "Two objects";

  const relation = "connected to";

  const points: string[] = ["A", "B"];

  const links: string[] = ["A:B"];

  return { name, points, links, relation };
}
